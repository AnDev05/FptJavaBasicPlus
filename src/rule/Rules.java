package rule;

/**
 *
 * @author Admin
 */
public class Rules {
    
    /*
     * Name of project must be upper case for each first character
     * EX: FptJavaBasicPlus or LearnJavaBasicPlus
    */
    
    /*
    * Name of package must be lower case
    * EX: util or datatype
    */
    
    /*
     * Name of Class must be upper case for each first character
     * EX: Student, Calculator
    */
    
    
    /*
     * Name of Abstract Class must be upper case for each first character and add Abs sutitle
     * EX: AbsAction, AbsListener
    */
    
    /*
     * Name of Interface must be upper case for each first character and add I subtitle
     * EX: IConnector , IAction , IListener
    */
    
    
    
    /*
     * Name of varriable must be follow the basic rule and camel style 
     * EX: myStudent, count, myCalculator
    */
    
    /*
     * Name of constant varriable must follow the basic rule and be upper case
     * EX: PI , MY_CONSTANT
    */
    
    
    /*
     * Name of method must follow the basic rule and camel style
     * EX: calculate(), checkData() , getData()
    */
    
     /*
     * Name of method must follow the basic rule and camel style
     * EX: calculate(), checkData() , getData()
    */
  
}
